{
  "name": "fireman-pushgateway",
  "description": "kinvey prometheus push gateway",
  "version": "0.2.0-dev",
  "keywords": [ "prometheus", "monitoring", "metrics", "kinvey" ],
  "private": "true",

  "dependencies": {
    "config": "1.16.0",
    "express": "4.16.2",
    "qerror": "0.2.0",
    "qfputs": "1.7.1"
  },
  "devDependencies": {
    "mocha": "3.2.0"
  },
  "scripts": {
    "test": "mocha test",
    "coverage": "myc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output/ coverage/"
  }
}
