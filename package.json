{
  "name": "prom-pushgateway",
  "description": "embeddable aggregating prometheus push gateway",
  "version": "0.6.1",
  "keywords": [ "prometheus", "monitoring", "metrics", "push", "aggregating" ],
  "license": "Apache-2.0",
  "private": "true",
  "engines": {
    "node": ">=4"
  },

  "dependencies": {
  },
  "devDependencies": {
    "qnit": "0.22.0"
  },
  "scripts": {
    "test": "qnit test",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output/ coverage/"
  }
}
