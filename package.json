{
  "name": "prom-pushgateway",
  "description": "embeddable aggregating prometheus push gateway",
  "version": "0.6.3",
  "keywords": [ "prometheus", "monitoring", "metrics", "push", "aggregating" ],
  "author": "Andras",
  "license": "Apache-2.0",
  "repository": "https://github.com/andrasq/node-prom-pushgateway/#readme",
  "readme": "https://github.com/andrasq/node-prom-pushgateway/#readme",
  "engines": {
    "node": ">=4"
  },

  "dependencies": {
  },
  "devDependencies": {
    "qnit": "0.22.0"
  },
  "scripts": {
    "test": "qnit test",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output/ coverage/"
  }
}
