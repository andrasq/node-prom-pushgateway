{
  "name": "fireman-pushgateway",
  "description": "kinvey prometheus push gateway",
  "version": "0.3.0-dev",
  "keywords": [ "prometheus", "monitoring", "metrics", "kinvey" ],
  "private": "true",
  "engines": {
    "node": ">=4"
  },

  "dependencies": {
    "config": "1.16.0"
  },
  "devDependencies": {
    "qnit": "0.21.1"
  },
  "scripts": {
    "test": "qnit test",
    "coverage": "nyc --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output/ coverage/"
  }
}
